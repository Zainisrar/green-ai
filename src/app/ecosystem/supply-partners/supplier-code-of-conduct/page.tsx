import React from "react";
import Handbook from "@/app/components/Handbook/Handbook";

const page = () => {
  return (
    <React.Fragment>
      <Handbook />
    </React.Fragment>
  );
};

export default page;
